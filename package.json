{
  "name": "handle-bars-hm",
  "version": "1.0.0",
  "description": "idk",
  "homepage": "https://markiianperliak.github.io/handle-bars-hm/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel src/index.html",
    "build": "parcel build src/index.html --no-cache --public-url /handle-bars-hm/",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build"
  },
  "author": "Markiian",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.14.4",
    "parcel-transformer-hbs": "^1.0.4"
  }
}
